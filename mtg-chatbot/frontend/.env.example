# Base URL of the chat backend.
# Local development points at the FastAPI dev server (or the mock server).
# In production this is the deployed Lambda Function URL.
VITE_API_URL=http://localhost:8000
